# 03 — NAMING CONVENTION

## File PHP
Dùng `snake_case`:

```text
change_password.php
process_login.php
pending_sets.php
```

## Hàm PHP / JavaScript
Dùng `camelCase`:

```php
getUserByEmail()
createFlashcardSet()
calculateStudyStreak()
```

## Biến PHP / JavaScript
Dùng `camelCase`:

```php
$userId
$setId
$fullName
```

## Database
Dùng `snake_case`:

```text
user_id
set_id
created_at
study_history
```

## CSS class
Dùng `kebab-case`:

```css
.profile-card
.admin-table
.flashcard-item
```

## POST field
Ưu tiên giống database:

```text
email
password
set_id
question
answer
```
